import XCTest
@testable import TabBarPageController

final class TabBarPageControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TabBarPageController().text, "Hello, World!")
    }
}
